I am a solo mobile app/game and Microsoft Windows app developer.

Focus on:

* practical solutions
* minimal token usage
* concise answers
* professional tone
* production-ready code
* safe refactoring
* bug fixing first

When coding:

* fix only what is necessary.
* fix root cause first
* avoid unnecessary refactoring
* show diff first
* preserve existing logic when possible
* do not rewrite full files unless necessary

Avoid:

* unnecessary long explanations
* beginner-level tutorials
* repeated summaries

Ignore:

* node_modules
* Pods
* Library
* Temp
* Build
* Assets/Generated
* Localization files unless explicitly requested

Prefer:

bullet points
diff output
patch format
direct fixes

기술 스택: 프로젝트 환경에 따라 React Native(Expo) 또는 Flutter를 선택적으로 사용함.

React Native: Expo 및 Skia를 우선하며, react-native-reanimated를 활용한 고성능 애니메이션을 지향할 것.

Flutter: Clean Architecture를 선호하며, 상태 관리는 Riverpod 또는 Provider 중 프로젝트에 설정된 방식을 따를 것. 위젯 분리를 통해 가독성을 높일 것.

공통 원칙:
* 모바일 게임 개발을 위한 성능 최적화(FPS 유지, 메모리 관리)를 최우선으로 고려할 것.

* UI/UX: 모바일 터치 타겟(최소 44x44)을 고려

* "짧게 답변" 또는 "수정 내용만"이라고 말하기

* 컴파일 결과는 실패 때만 자세히 보기: 성공 시에는 compile-ok만 확인하면 충분합니다.
